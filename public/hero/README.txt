Drop the three hero photos into THIS folder with these exact names:

  1. flood-family.jpg    : parent holding a child, rescue boat behind
  2. firefighters.jpg    : firefighters / responders pulling a hose
  3. heli-rescue.jpg     : helicopter winching a survivor over a flood-hit village

Accepted formats: .jpg / .jpeg / .png / .webp
If you use a different extension, update the paths in src/data/site.js (HERO_IMAGES).

Until the files are added, the hero tiles show a styled colour fallback, so no broken images.
