# Imported Django Projects

This folder preserves the Django projects imported from the provided zip files.

## Folders

- `hazard_map_project`: hazard-map copy.zip
- `disaster_rescue_management`: disaster-rescue-management.zip backend
- `id_generation`: umeed.zip(drid gneration).zip backend

These projects are kept separate from the root FastAPI starter in `backend/app.py` so their settings, apps, migrations, and SQLite databases remain intact.

## Frontends

The static frontends from the zip files are available under:

- `public/legacy/disaster-rescue-management`
- `public/legacy/id-generation`
